{% for server in __ntp_server  %}
pool {{ server }}   iburst maxsources 1
{% endfor %}
keyfile /etc/chrony/chrony.keys
driftfile /var/lib/chrony/chrony.drift
logdir /var/log/chrony
maxupdateskew 100.0
rtcsync
makestep 1 3
